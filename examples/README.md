# Examples

This directory contains runnable examples for `@kairoguard/sdk`.

## Available examples

- `evm-sign-with-policy`: demonstrates creating a wallet, computing EVM intent, and building a Sui receipt transaction.

## Running an example

```bash
cd examples/evm-sign-with-policy
npm install
npm run dev
```

Set the required environment variables before running.
